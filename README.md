# 贝塞尔根系重建（matlab / 图片+位姿 → 重建图与数据表）

围绕 `plantroot06.m`（三维贝塞尔树状根系，向下生长）实现的**根系三维重建工具集**。既能**从 matlab 文件**、也能**从 24 张图 + 相机位姿**出发，还原出根系模型，并输出**重建图**与**数据表**（空间坐标表 / 贝塞尔定义点表 / 根表）。

提供两种使用方式：

1. **Web 端程序**（推荐）：纯 Python 标准库实现的 Web 服务，三个输入模块，浏览器点选文件即可出结果。
2. **命令行脚本**：直接跑 Python 脚本。

数据表明细：节点图结构，每条根的起点/终点都锚定在**分支点/末端点节点**上；曲线由一组贝塞尔定义点（端点 + 控制点）确定。

---

## 一、Web 端程序（开箱即用）

零依赖（仅使用 Python 标准库 `http.server`，无需 Flask），三个输入模块：

| 模块 | 输入 | 输出 |
|---|---|---|
| **模块①** matlab → 24 图 + 位姿 | `plantroot06.m` | 24 张连续曲线图（15°/张）+ 相机位姿文件 `pose.json` |
| **模块②** matlab → 模型根表 | `plantroot06.m` | 根表 `root_table.csv/.sql` + 重建图 |
| **模块③** 24 图 + 位姿 → 重建 | 24 张图 + `pose.json`（可选） | 空间坐标表 `reconstructed_points.csv` + 贝塞尔定义点表 `control_points.csv` + 重建图 + 对比图 |

### 启动

```powershell
python web_server.py
```

浏览器访问 `http://127.0.0.1:8090/`。

> Windows 下如中文乱码，先执行 `chcp 65001` 并设 `$env:PYTHONIOENCODING='utf-8'`。

### 接口（后端）

| 方法/路径 | 作用 |
|---|---|
| `POST /api/from_matlab` | 模块②：matlab → 模型 + 根表 + 重建图 |
| `POST /api/from_images` | 模块③：24 图(+pose) → 空间表 + 贝塞尔图 + 对比图 |
| `POST /api/gen_views` | 模块①：matlab → 24 图 + 位姿文件 |
| `GET /results/<path>` | 下载生成的图/表 |
| `GET /api/zip_views/<job>` | 把某任务的 24 图 + pose 打包成 ZIP 下载 |

入口③支持**上传位姿文件**：读取 `pose.json`（含每台相机的 K/R/t/P）重构相机；若不传，则用默认已知参数。

---

## 二、命令行脚本（可选）

### 从 matlab 生成模型与根表

```bash
python make_table.py        # 模型 → 根表(root_table.csv/.sql) + 由表反推绘图 + 验证精度
python fit_and_rebuild.py   # 对每条曲线做最小二乘贝塞尔拟合重建 + 回填根表
python reconstruct_from_table.py root_table.csv   # 只凭表还原(独立脚本)
```

### 从 24 图 + 已知相机做重建（图→表→贝塞尔）

```bash
python build_table_from_images.py   # 24图 + 已知相机 → 颜色标色 → 三角化 → 空间表 → 贝塞尔重建
python render_rebuilt_compare.py    # 生成"左原模型 / 右重建"同视角对比图
```

### 生成 24 张连续曲线渲染视图（5°/张或15°/张）

```bash
python render_all_views.py          # 生成连续曲线渲染图(24 视角, 用于展示)
```

### 其它

```bash
python render_side_by_side.py       # 原模型 vs 拟合重建 同视角并排
python pipeline.py                  # 多视角三角化演示(指定点)
```

---

## 三、相机位姿（默认参数）

入口③默认相机参数（模块①生成的 `pose.json` 即为这套）：

```python
CAM_RADIUS, CAM_HEIGHT = 15.0, -2.0
CAM_TARGET = [0.0, 0.0, -7.0]        # 看向模型中心
FX, FY, CX, CY = 600.0, 600.0, 320.0, 340.0
N_VIEWS = 24                          # 15°一张, 环视一圈
up = (0, 0, -1)                       # 让根显示为"地表在上、深处在下"(根朝下)
```

---

## 四、数据表

### ① 空间坐标表 `reconstructed_points.csv`（三角化出的曲线上点）

```
点序号 | 曲线id | 段内序号 | 类别 | x | y | z
```
`类别` = 起点 / 中间点 / 终点。每条曲线 12 个点，共 120 点。

### ② 贝塞尔定义点表 `control_points.csv`（每条曲线的端点 + 控制点）

```
曲线id | 控制点序号 | 类别 | x | y | z
```
每条曲线反解出一组贝塞尔控制点（5 次 = 6 个：2 端点 + 4 控制点）。

### ③ 根表 `root_table.csv`（模型 → 表，节点行 + 曲线行）

```
序号 | 级别 | 编号1 | 编号2 | 编号3 | 编号4 | 类别 | x | y | z | 粗细 | 成熟度 | 上节点 | 下节点
```
- `级别`：根分支层级（1=主根 …）
- `编号1`：所属曲线/分支段 ID；`编号2`：点在曲线内序号
- `类别`：`节点/分支点/末端点/首节点/控制点1/控制点2/末节点/采样点`
- `上节点/下节点`：起点/终点所连接的分叉节点（对应 Node 结构）
- `粗细`：半径（沿曲线渐变）；`成熟度`：归一化成熟度

---

## 五、核心流程（图 → 表 → 贝塞尔重建）

1. **渲染**：24 台相机，每条根取 12 个曲线上点（起点/中间/终点），每点一个唯一颜色（贪心最远点选色，颜色彼此远离）。
2. **标色**：模型几何在图上叠加彩色标记。
3. **匹配**：每视角按最近调色板色定位各点 2D 质心（下采样 + 分块加速）。
4. **三角化**：多视角 DLT，**带视角一致性剔除**（剔除重投影误差大的配错视角）。
5. **空间坐标表**：反求出每点 3D 坐标 → `reconstructed_points.csv`。
6. **贝塞尔反解**：按曲线分组，用曲线上点最小二乘反解 5 次贝塞尔控制点 → `control_points.csv`。
7. **重建**：每条曲线一条平滑贝塞尔，**粗细沿曲线渐变**（主根粗→末端细），**节点连续**（相邻段共享节点对齐）。

---

## 六、精度

| 场景 | 结果 |
|---|---|
| 模型 → 表 → 拆控制点还原 | 最大偏差 0.0001（相对 0.0004%） |
| 20 张图三角化重建点 | 中位误差 0.014，最大 0.12（带视角剔除后） |
| 单段三次贝塞尔拟合 | 主根约 3% 几何误差（可改 5 次/分段改善） |

---

## 七、环境

- Python 3.10+
- numpy、Pillow（PIL）、matplotlib、scipy
- 不需要 OpenCV（颜色标记代替特征匹配）、不需要 Flask（Web 用标准库）

```bash
pip install numpy pillow matplotlib scipy
```

---

## 八、限制与说明

- **入口③纯曲线图来自已知模型**（闭环：模型几何生成颜色标记再三角化）。若图是真实无模型照片，需另做特征检测/匹配。
- **控制点数值发散**属正常——5 次贝塞尔的中间控制点不在曲线上、由最小二乘反解，但整条曲线精确穿过测点并贴合原模型。
- 中间控制点为反解值，可能不直观；曲线本身准确。
